// import { Sun } from "lucide-react";

export const PageHeader = () => {
  return (
    <header className="page-header">
      <div>
        <h1>
          Convert <span>IEEE</span> PDF to XML
        </h1>

        <p>
          Upload your IEEE formatted PDF document and convert it to IEEE XML
          format.
        </p>
      </div>

      {/* <button className="theme-top-button">
        <Sun size={18} />
        Theme
      </button> */}
    </header>
  );
};
